# 10. What are controllers, services, repositories, middlewares and request context?

## Notes

- in this video we are going to cover three topics handl services and repository pattern middle a and request context initially the plan was to cover each Topic in different different videos so this was supposed to be three videos but since these three topics are so closely related with each other in fact pretty much all the topics are closely related to each other since we are talking about
- to your mind that you have to take out the data from this request object that is the first responsibility of a Handler of a controller so this is what you have to keep in mind again this is not a necessary requirement you can do everything in your Handler SL controller but in a typical backend architecture this is how how we structure it to make it more skillable
- all the data that the client has sent it is in the expected format all the mandatory values are sent all the values that are sent are in the expected format there is no malicious intent there is no uh missing values etc etc everything that we do in a validation pipeline after we validate it we also optionally do a transformation what does transformation mean transformation is basically after
- data it might be receiving the email address and it takes that email address it executes the logic which sends the email to the client and it then returns a response that looks something like this success is true to your Handler back it can also do something like this without calling the repository method we also have services like this or if it needs to deal with databases whether
- the same context now where do middleware come in in this whole picture of a request life cycle now let's do the whole request life cycle again but this time we'll use middle ver and we'll understand how middle W work and where do they actually come in okay we have a client let's not imagine client now let's say we get the request from a client now the moment
- different places that's the reason we use functions in programming same way we use middleware so that we can do some logic in our whole API request life cycle again and again in different different request so as a backend application your app receives thousands or depending on the scale depending on the popularity of your platform it might be receiving thousands and millions and billions of requests every day
- and Etc then it passes that to the next execution environment or for the next middleware okay if it does not match then we don't add any headers and the browser will block the request by default when we send the response back to the client okay now the question is why did we make this as a middleware because this is a function that needs to happen that needs
- 500 errors and it can send a properly structured error message which usually has a message or some kind of code so that the front end can render different different uh userfriendly messages depending on the code or the ID sometimes also called ID and the status back to our client so the goal of the global error handling middleware is it takes out an unstructured error from anywhere from
- we have routing then we have authentication middleware then we have some kind of middleware that is limited to the application okay some kind of permission checks then we have the Handler logic then at the end we have Global error handling mod etc etc until response is sent back to the client we have different different middlewares now a context a context is typically some kind of storage usually
- typically have in a backin server so that's all about uh handlers Services repositories middlewares and request context
