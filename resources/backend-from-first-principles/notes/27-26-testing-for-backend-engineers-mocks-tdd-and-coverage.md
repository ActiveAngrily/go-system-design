# 26. Testing for Backend Engineers: mocks, TDD and coverage

## Notes

- Over the last 20 or 25 videos , we have learned a lot of concepts . And in all of that , we have never written a test . That was intentional . And we'll see why in this video . Because writing a bad test , writing a test in hurry , writing an incomplete test is more harmful than not having tests at all . Because they give
- gives us the test binary . It prints the file the line number and what it wanted versus what it got . Uh the task it got was in doing and the test wanted it done . Now we fix the expectation and let's run it again and it passes instantly . Now let's look at what is happening behind it . I run it with dash work which tells
- to end test they both sound similar but the difference is an end toend test will have to cover every single path which means that what is the first thing that a user need to do to start using our platform which is authentication . So the first part of your end to end test will have to include authentication and then it will take that token that session whatever
- in the uh previous video in the realtime systems how events propagate from the client to the server through the event bus to all the other clients . So in a real back end that particular handler it also has to write to the database posgress and then it might also have to call a payment provider um to check their credits or something like that and then it might
- whatever it is in your programming language which has your own applications structures your own success objects failure object error messages everything of your own . And then you use that wrapper in your main workflow in your production and also during testing . And now that you own the interface , you are allowed to test it . That is the first thing you should do . Second thing
- wrap the external service or the external layer with your own interface your own API your own wrapper and then in your test layer you try to mock that or fake that but there is a second technique here which is useful when you actually want to test the HTTP call . You want to test your own client code . You also want to interact with your headers ,
- green you are expected to and you must come back to the code and you have to refactor it because the reason you are allowed to write bad code or not care about best practices in second step so that you can come back to it in step three you cannot skip this part . Okay . Now let's see what actually testdriven development looks like . We have a
- that we saw . And what was the fix for that which was instead of trying to fake a sleep you wait for a signal you wait for a condition not the time itself . Then for concurrency we have u things like sync test . if you are using Go and other libraries and frameworks have their own primitives for synchronous testing . But the third one is new
- as a score is 52 63 % . which means that nine out of those 19 they were able to survive our tests did not catch those damages . So if you look at the second survivor the second mutant that was able to survive the in the loop it was I less than or equal to offset plus limit . What striker did it changed it to I less
- are using Go , then it also has its own lint . But other than that there is no particular tool or deterministic way to decide whether a code is of high quality or
