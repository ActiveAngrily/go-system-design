# 23. Object Storage - Everything You Need to Know: Part -1

## Notes

- Okay , so far we have talked about a lot of things . You have built a back end and it works well on a lot of ends . It has authentication , it has database , it has your handlers , your services , your repositories , pretty much everything that we have discussed so far in this playlist . Now let's imagine a real life scenario where a
- of backend engineering where cost is something that you have to keep in mind when you are dealing with files . Now , as usual , before we get to the core topic , we have to cover a little bit of the surrounding area . Before before we talk about object storage and what it actually is , let's spend some time on why it exists . Because if
- in our tech industry , the only solution to durability is replication , which is just a fancy term of keeping some piece or some backup of your data somewhere else . So at any point there should be multiple copies of your data and the moment you have these backups these copies you also have things like check sums and you also need background processes that can detect when
- a lot of system operations will immediately break . A lot of applications and systems depend on this property of our file system that a rename will be completely atomic in the sense it will either go successful or it will fail . There should not be any in between corrupted partial state . Similarly , a partial write it has to be visible consistently . What does that mean
- in the object storage . All of these objects they live inside a bucket . A bucket is the term that we use which is not exactly a folder but it is something that exist one level up of objects . Objects are stored inside a bucket . So bucket is just a named container and the bucket name combined with the key of the object it has to be
- with a 100 MB file , what exactly happens behind the scenes ? The first thing that happens is your request lands on one of the thousand thousands or millions stateless front - end nodes that is served by AWS S3 . Since we are talking about AWS S3 primarily , it's one of the most famous object stoages . So , one of those nodes authenticates the request which
- the preventions that you can do to avoid cases like that . Okay . Now let's talk about the hard engineering problem which is the metadata plane . Now conceptually speaking it is just a huge sorted key value index . You can imagine it as a distributed B tree also called as a balance tree . If you are familiar with data structures and algorithm concepts you might have
- hear a lot in the object storage ecosystem . S3 compatible . What that means is in one line that roughly speaking that they support the same protocol as AWS S3 . For example , Cloudflare R2 is called S3 compatible because you can use the AWS SDK to interact with Cloudflare's ecosystem . Cloudflare R2 to be precise . That is what people mean when they say S3 compatible
- completely solves our problem which is the credential never leaves our server but the browser gets the capability to communicate with our bucket without any security risk . to see a demo for this presign URL thing . We have a page here , a front - end app which is talking to S3 through our server . So first thing is the page asks our own server for a
- cover the use of the E tag header in the next section where we'll need this header for the multi - art uploads also . So if your upload works in curl in your terminal or in Postman but it fails in your browser then most likely it is because of the course and you should probably check your bucket's course policy .
