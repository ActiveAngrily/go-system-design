# 24. Object Storage - Everything You Need to Know: Part - 2

## Notes

- Everything we have done so far works great for a few hundred megabytes . Now let's talk about large files and we will see why a single put request stops working and there can be around four separate reasons for that . Reason one is hard limit which means that a single put request a single upload request cannot exceed more than 5GB and this condition is not enforced by
- object has not been created yet until the very end where we will commit . Now step two which is upload part which is once per chunk you send the upload ID a part number and the bytes and the important property here is all of these are completely independent HTTP requests and because of that they can go in parallel and they can go in any order which means
- choosing the part number . I personally use around 16 or 32 mgabyte as a floor which means that it can handle files up to a 160 or 320 GB without thinking a lot about the formula and all . So why not just use huge parts like big parts ? The reason is because the part is going to be your retry granularity which means that if a single
- very good default . It it does not really cost you anything and it permanently uh closes a leak . So very important thing to keep in mind once you start uh using multipart uploads . Now that we have learned about both and multipart , now it's time to combine them because that is what we use in production for uploading large files from web from our browsers .
- response header in the buckets in your S3 buckets course config otherwise your browser your JavaScript cannot read the E tag of the response that the bucket sends back and you cannot complete the upload . And after we do all of these the capability to resume a big upload we get that automatically . So if the page is refreshed the client our browser it asks our API this
- to the the right solution which is CDN with caching that we'll talk about soon which has zero bandwidth cost because the data does not travel through your server but it also offers very fast downloads why because CDN servers are usually spread across the whole world in every region in every country as compared to a server which is maybe in a particular region or two three different regions
- the whole authorization process happens at the edge of the network and the city itself has full control over the caching instead of the back end being involved . these uh signed cookies that I mentioned , these are very convenient especially for video because they're a single session of video . It pulls hundreds and thousands of segments of files because the video is being streamed and because of
- to a hex dump using a tool called XXT , what we get is a PNG header . And now because of the whole interaction , because of the range requests and us being able to read a part of the file , we know that this file is a PNG file without downloading the whole PNG , without downloading the whole image . Now here it looks very basic
- all in all there are three different things that you are built for that you pay for . First is the storage which is measured as per gigabyte per month . That is the number one . Then comes operations which is per request . How many requests that you are making to the object storage . And in requests also the writes the the put requests or any kind
- do resuming and parallel download and all this stuff and object storage natively supports that . For videos , we try to segment it and we have specialized players to adapt based on the segments . That's pretty much the whole recap .
