# 27. The Twelve-Factor App

## Notes

- So far in this playlist , I have taken a lot of decisions and prescribed a lot of ways of how a back end or an application should be built and I have never really stopped to defend them . For example , why configuration goes in environment variables , why the server binds a port , why logs go to the standard output , why processes have to be
- And that is where this famous phrase called it works on my machine got popular and people used to schedule their time for any kind of deployment work by freezing any other work that any of the team members were doing . And this document the 12 actors document it has a name for like this for an app that is built and operated like this . It is called
- places not the other way around which means the copypaste sharing of code that is the antipattern here . Then comes factor two explicitly declare and isolate dependencies . Two important words there declare and isolate . Declaration means that somewhere in your repository there is a file which lists completely and accurately every library that your app depends on that your app needs to run in your environment .
- database . yiml . Every framework they had their own format and pretty much all of them at some point or another they used to get committed into the source control and credentials used to get leaked . If we look at our own codebase , one of the code bases that we wrote for one of the videos in this channel which is called tasker which is a task
- in the same server instance is considered a antiattern when it comes to scaling . Does not matter what database that you use whether RDS or neon planet scale or even other services like for radius also we use managed red instance . For object storage we use S3 for emails instead of custom SMTP service used managed services like resend . So this factor aged very well . In
- working . It does not break . So what kind of scenarios ? Let's say your application stores the logged in user session in process memory . So it works perfectly in one instance . Then if you have watched the scaling video after that let's say you scale your instance to three different instances to handle your load and you place all of them behind a load balancer and
- runtime itself . And during the startup , it used to take a huge block of memory , huge block of RAM . And then it used to multiplex inside itself , inside its own runtime with threads . That's how it used to scale . PHP was the opposite of that . The Apache server on top of which PHP used to run it used to spin small child
- if it does not follow a particular life cycle of uh finishing the existing work then stopping to take any new work and then stopping the process and all these uh things that we discuss in the graceful shutdown part then all the in - flight requests they suddenly get errors when you do any kind of deployment . So as we discussed in the graceful shutdown video whenever we
- is not the same implementation because AWS is Dynamob it is proprietary and whatever you learn about the behavior is from the documented ones so you cannot really replicate the exact technology in your local so that does create some gap and there is no straightforward advice here that you should not go for managed services and you should try to keep the environment same in local production . You
- admin process which is uh for autoarchchiving which runs from the same code the same config and the same environment and then it exits and in the end we press Ctrl C and if you look at the shutdown the connection pool closes the job server waits for all its workers to finish their jobs and the process reports a clean exit is the factor 8 9 and Elsewhere
