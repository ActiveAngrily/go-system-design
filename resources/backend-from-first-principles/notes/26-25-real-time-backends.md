# 25. Real-Time Backends

## Notes

- At this point , you have built a fairly complete back end authentication , database handler , services , repositories , caching , background jobs , and in the last two videos also object storage . And through all of it , every single thing our back end has ever done has followed exactly one shape , which is the browser asks a question and the server answers it .
- that you're choosing . In this case it is 3 seconds . So the average delay will be around 1 . 5 seconds . So what is the solution to that ? Pull faster . Do not keep a delay which is visible to the user . Make it 1 second . So let's see what is going to break when we make the polling interval to 1 second .
- that is wrong with the solution . But to summarize the the the reason this solution is wrong is the gap that happens there after the server answers the browser has to process the whole response then open a fresh request to the server and during that whole window the server has no connection to you and according to this RFC the average latency is about one network transit but
- look at LinkedIn's instant messaging feature which also has these typing indicators and read receipts , they also used server sent events . And the reason is since it is all they did not have to think about compatibility between different different devices and environments . It just worked everywhere . And in this era of agentic coding , all of us pretty much use different different kinds of CLI
- . So once that exchange is done the part is over the connection still stays open but it is not HTTP anymore . Everything changes . No headers , no methods , no status codes . It is now a completely new birectional stream of data and both sides can write whenever they want . And this birectional stream of data it is also called birectional stream of frames .
- is just there holding a socket and it it believes that the client is still there and it will keep holding on to it for a very long time because from the server's point of view from the sockets's point of view an idle connection and a dead connection looks exactly the same because nothing is happening there . So the only way to tell them apart is to send
- this time with a completely different error which is cannot assign requested address . So to investigate this , let's ask the kernel itself which port numbers it is allowed to use when it makes an outgoing connection . And it says 32 , 768 up to 60 , 999 . If you subtract and add one that is 28 , 232 and we have to subtract 42 because this
- a go routine and a note somewhere that this particular socket is involved with board 412 . Now instance 2 has never heard about browser A and it cannot because there are two separate process and probably in two different countries . We spent pretty much our whole playlist making our backend stateless so that any instance can serve any request . But now in this video we're saying that
- with increasing sequence numbers . If at any point the connection breaks , the client reconnects and says I was able to get up to sequence number two . So the server takes that and then it starts from three because that's where the client was errored out . This is exactly what we saw with the server send events using the header last event ID . Now the last
- Like if you are talking about the collaborative editing space then you have to understand algorithms like CRDT how they work over the wire and all those stuff . But everything that we talked about in this video is the foundation on top of which we can keep adding more and more capabilities .
